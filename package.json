{
  "name": "food-waste-root",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "npm --prefix ./foodbridge/client run dev",
    "install-client": "npm --prefix ./foodbridge/client install",
    "start-client": "npm --prefix ./foodbridge/client run start"
  }
}
